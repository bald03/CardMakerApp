import React from "react";
import { TextBlockProps } from "./types";

const TextBlock: React.FC<TextBlockProps> = (props) => {
  return (
    <div>
      <p>TextBlockElement</p>
    </div>
  );
};

export default TextBlock;
